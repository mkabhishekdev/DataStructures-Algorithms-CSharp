a)
      git status 
      git add *
      git commit -m "comment"
      git push

b) When your local branch and remote branch have diverged, do below:
      1) git pull origin main --rebase
      2) git add .
      3) git commit -m "your message"
      4) git push origin main

c) 
      1) To check the branch you are in: git rev-parse --abbrev-ref HEAD
      2 )To check the last commit status:  git log --name-status HEAD^..HEAD


d)
      Cherry-picking:
      ****************
      Cherry picking in Git means to choose a commit from one branch and apply it onto another.
      1) Make sure you are on the branch you want to apply the commit to.
        git switch master

      Execute the following:
      2)git cherry-pick <commit-hash> (commit-id can be found in the PR)

 
